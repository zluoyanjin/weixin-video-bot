# 微信视频号机器人 · 使用说明（客户版）

> 整个文件夹可以放到任意位置（C 盘 / D 盘 / 桌面 / U 盘都行），程序里没有任何写死的路径。

## 一、一句话上手

```
双击「安装环境.bat」  →  双击「配置账号.bat」  →  在弹出的窗口里：
                        ① 填账号和文案  ② 点「启动 Chrome」登录  ③ 点「启动机器人」
```

**客户全程只跟 2 个 .bat 文件打交道**：`安装环境.bat`（只用一次）和 `配置账号.bat`（天天用）。
开浏览器、开机器人、停机器人，都在「配置账号」这个窗口里点按钮完成，不用再碰其它 .bat。

---

## 二、文件夹里都有什么

```
微信视频号机器人/
├─ 安装环境.bat      ★ 第一次使用双击一次（装组件）
├─ 配置账号.bat      ★ 天天用：改配置 + 开浏览器 + 开/停机器人（全在这里）
├─ 启动Chrome.bat    可选：和界面里的「启动 Chrome」按钮一样，给习惯用黑窗口的人
├─ 启动机器人.bat    可选：和界面里的「启动机器人」按钮一样
├─ 自检.bat          可选：交付前自检，不用真实账号也能验全链路
├─ 体检.bat          可选：真实账号体检，只连接不发送
├─ 打包成exe.bat     开发用，客户不需要
│
├─ app/              程序本体（不用动）
├─ config/
│   └─ accounts.json  ★ 所有配置都在这一个文件里
├─ chrome_profiles/  登录状态（自动创建，不要删）
└─ logs/             运行日志（自动生成）
```

---

## 三、第一次使用（两步）

### 1. 安装环境（只做一次）
双击 `安装环境.bat`，等黑色窗口跑完（第一次会下载 Playwright，需要几分钟），
看到「安装完成」按任意键关掉。
前提：电脑已装 **Chrome**，以及 **Python 3.10+**（安装 Python 时勾选 **Add Python.exe to PATH**）。

### 2. 配置账号 + 登录 + 开机器人（全在一个窗口里）

双击 `配置账号.bat`，会弹出一个图形窗口（这就是操作界面，不是黑窗口）：

1. **填账号**：左边选中一个账号，中间填：
   | 字段 | 说明 |
   |---|---|
   | 账号名称 | 随便起，用来在日志里区分 |
   | CDP 端口 | 每个账号一个，**不能重复**（9222 / 9223 / 9224…） |
   | 发送间隔(秒) | 建议 **≥ 300 秒**，太快容易被平台限制 |
   | 启用 | 不勾就不发 |
   | 直播间地址 | 选填，填了机器人会自动帮你打开该直播间 |
   | 每轮发全部话术 | 勾选(默认)：每次到点把文案框里**所有行按顺序依次发完**；不勾：每条按间隔轮流发一条 |
   | 发送文案 | 一行一条；默认每轮把全部行依次发出（即「到点把每一行都发一遍」） |

2. **点「启动 Chrome」**：会依次弹出 Chrome 窗口，每个对应一个账号。
   **分别登录不同的视频号，并进入要发言的直播间**（窗口上会显示绿底标签，写明这是哪个账号、对应哪个端口，方便核对）。

3. **点「启动机器人」**：开始按间隔自动发送。窗口下方的「运行日志」会实时显示发送情况。
   想停下来：点「停止机器人」按钮，或直接关掉这个窗口。

> 登录状态保存在 `chrome_profiles/`，**第一次登录之后就不用再登录了**；
> 网页登录的账号、保存的密码、Cookie 都会持久化在这里。
> **注意：千万不要删除 `chrome_profiles/` 文件夹，否则登录态会丢失、需要重新登录。**

---

## 四、以后每天怎么用

```
双击「配置账号.bat」  →  点「启动 Chrome」（登录好的窗口自动打开）
                      ↓  进直播间
                      点「启动机器人」  →  自动定时发送
```

如果上次没关 Chrome、登录态还在，点「启动 Chrome」会直接复用，不用重新登录。

---

## 五、常见问题

**Q：提示「没有连上任何视频号」**
按顺序排查：① 有没有点「启动 Chrome」；② Chrome 里是不是停留在视频号直播间页面；
③ `config/accounts.json` 里的端口和账号是不是对应同一套（9222/9223/9224）。

**Q：提示「找不到消息输入框」**
当前页面不在直播间，或直播间评论框还没加载出来。切到直播间页面即可。

**Q：想先测试，不想真发**
在窗口里勾选「演练模式（只打印不发送）」，照常点「启动 Chrome」「启动机器人」，
这时只在日志里打印「模拟发送」，不会真发。

**Q：换电脑 / 换人用**
把整个文件夹复制过去，**先清空 `chrome_profiles/` 里面的内容**，让对方重新登录自己的视频号。
不要把登录状态一起拷过去。

**Q：日志在哪**
`logs/日期.log`，每次发送成功 / 失败都会记录。

---

## 六、Mac / Linux 用户注意

`.bat` 是 Windows 专用，Mac / Linux 请用同名的 `.sh` 脚本（功能完全一致）：

```
bash 安装环境.sh      # 装 Playwright
bash 配置账号.sh      # 开配置窗口（之后在里面点启动 Chrome / 启动机器人）
```

- `app/chrome.py` 已内置 Mac / Chrome 路径探测；Chrome 装在非标准位置就在 `config/accounts.json` 顶层加 `"chrome_path": "/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"`。
- 若报 `No module named '_tkinter'`：macOS(brew 版) 执行 `brew install python-tk`，Linux 执行 `sudo apt install python3-tk`。
- 登录态同样存在 `chrome_profiles/`，勿删。
- 完整说明见仓库 `README.md` 第十节。

---

