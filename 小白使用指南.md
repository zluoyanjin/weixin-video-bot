# 微信视频号机器人 · 小白使用指南（完全不用看代码）

你拿到的是一个文件夹，名字叫「微信视频号机器人」。
里面文件很多，但**你只要双击 2 个带 .bat 的文件**，其他都不用管。

---

## 一、第一次准备（只做一次）

1. **装 Google Chrome 浏览器**
   如果没有，先去装一个。机器人是挂在你的 Chrome 上工作的。

2. **装 Python（关键一步）**
   - 打开浏览器搜「Python 下载」，进 python.org 下载最新版。
   - 安装时**一定要勾选「Add Python.exe to PATH」**这一项（默认可能没勾，务必打勾）。
   - 一路点下一步装完。

3. **解压文件夹**
   把「微信视频号机器人」文件夹解压到你喜欢的地方，放桌面也行。

4. **双击「安装环境.bat」**
   - 会弹出一个黑色窗口，自动装一个叫 Playwright 的组件，等它跑完（第一次要几分钟）。
   - 看到「安装完成」后，按任意键关掉窗口。这一步也只做一次。

---

## 二、日常使用（就 1 个窗口搞定）

以后所有操作都在「配置账号」这个窗口里完成，不用再碰别的 .bat。

### 第 1 步：双击「配置账号.bat」
会弹出一个图形窗口（这就是操作界面，不是黑窗口）。

### 第 2 步：填账号和文案
- 左边选你要改的视频号账号
- 中间「发送间隔（秒）」填数字（比如 300 就是每 5 分钟发一轮）
- 下面大框里填发言内容，**每行一条**话
- 勾选「每轮把全部话术发一遍」（默认就勾着）：到点后把你写的所有行**按顺序一条条全发出去**，不会只发第一行

> 简单说：你写几行话，到点机器人就把这几行全部依次发出；不想一次性全发、只想慢慢轮一条，就把那个勾去掉。

### 第 3 步：点「启动 Chrome」并登录
- 窗口里点 **「启动 Chrome」** 按钮 → 会弹出几个 Chrome 窗口（每个窗口对应一个视频号）。
- 在**每个窗口里登录你的视频号**，并点进你要发言的**直播间**。
- 窗口上有绿底标签，写着「这是【账号名】窗口 · 端口 XXXX」，方便你核对哪个窗口是哪个账号。
- 登录好、直播间打开后，先放着别关。

### 第 4 步：点「启动机器人」
- 窗口里点 **「启动机器人」** 按钮 → 机器人开始按设定时间，自动往直播间发消息。
- 窗口下方的「运行日志」会实时显示发送情况。
- 想停下来：点 **「停止机器人」** 按钮，或直接关掉这个窗口。
- 注意：第 3 步的 Chrome 窗口**不要关**，关了机器人就连不上了。

就这么简单。登录状态会记住，第二天不用重新登录。

---

## 三、想改「发什么话 / 多久发一次」

随时双击「配置账号.bat」，改完点「保存配置」即可（改完不用重启，下次启动机器人就生效）。

---

## 四、想先试试、又怕乱发

在「配置账号」窗口里勾选 **「演练模式（只打印不发送）」**，照常点「启动 Chrome」「启动机器人」，
这时只在日志里打印「模拟发送」，**不会真的发出去**，用来确认账号、间隔、端口都对。

另外还有两个安全的测试入口（可选）：
- **双击「自检.bat」**：不用任何视频号账号，验证程序本身正常不正常。
- **双击「体检.bat」**：先按上面第 3 步打开 Chrome 并登录进直播间，再双击它。它只检查「能不能看到直播间的输入框」，**不会发任何消息**。看到「已找到输入框」就说明没问题。

---

## 五、常见问题

- **双击 .bat 一闪而过 / 报错**：先回头看第一步，Python 装好没？「Add Python to PATH」勾了没？
- **机器人说「找不到输入框」**：说明你还没点进直播间，或评论框没加载出来。回到 Chrome 窗口，点进直播间，再点「启动机器人」。
- **想多发几个视频号**：在「配置账号」窗口里点「新增账号」，填个名字和端口号（比如 9225），保存后点「启动 Chrome」就会多开一个 Chrome 窗口。
- **登录状态在哪**：存在文件夹里的 `chrome_profiles` 子文件夹。第一次登录后基本不用重登，除非账号掉线。

---

## 六、千万别做

- **不要用记事本去改那些 `.py` 文件**（名字带 py 的），改坏编码程序就跑不了。
- 不用管 `app` 文件夹、`config` 文件夹里的东西，那是给懂代码的人看的。
- **不要删除 `chrome_profiles` 文件夹**，否则登录态会丢、要重新登录。

---

你只和 2 个 `.bat` 文件打交道：**安装环境**（只做一次）和 **配置账号**（天天用）。
开浏览器、开机器人、停机器人，全在「配置账号」窗口里点按钮完成。
